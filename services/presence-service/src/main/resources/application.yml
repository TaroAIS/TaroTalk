server:
  port: 8086

spring:
  application:
    name: tarotalk-presence-service
  redis:
    host: localhost
    port: 6379

management:
  endpoints:
    web:
      exposure:
        include: health,info
